# Returns the nth element of the Fibonacci sequence, where n = 0 is the first.
def fib(n) {
    if n <= 1 {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

def $main() {
    for var i = 0; i < 20; i += 1 {
        echo fib(i);
    }
}
