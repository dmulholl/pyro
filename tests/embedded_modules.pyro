import std::testfile;
assert $is_module(testfile);
assert $is_str(testfile::path);

import std::testdir;
assert $is_module(testdir);
assert $is_str(testdir::path);

import std::testdir::testfile as testdir_testfile;
assert $is_module(testdir_testfile);
assert $is_str(testdir_testfile::path);
