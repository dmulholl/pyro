import std::math;
import std::constants;

assert math::abs(1) == 1;
assert math::abs(-1) == 1;

assert math::abs(1.0) == 1.0;
assert math::abs(-1.0) == 1.0;

assert $is_err(try math::abs(constants::i64_min));
