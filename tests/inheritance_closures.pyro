class A {
    pub def foo() {
        return "a-foo";
    }
}


class B < A {
    pub def foo() {
        return "b-foo";
    }

    pub def get_closure() {
        return super:foo;
    }
}


var b = B();
var closure = b:get_closure();
assert closure() == "a-foo";
