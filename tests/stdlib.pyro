assert !$is_module(123);

import std::math;
assert $is_module(math);
assert math::abs(-1) == 1;

var math_mod = math;
assert math_mod::abs(-1) == 1;

var abs_func = math::abs;
assert abs_func(-1) == 1;
