name: Build & Test

on: [push, workflow_dispatch]

jobs:
  run-tests:
    name: Run Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: List Dir
        run: ls

      - name: Build Pyro
        run: make debug

